import FormWrapper from "@/app/components/auth/formWrapper";

const ResetPassword = () => {
  return (
    <div>
      <FormWrapper type="reset-password" />
    </div>
  );
};

export default ResetPassword;
